export default function LoadingSpinner() {
    const LoadingSpinner = document.createElement('div');
    LoadingSpinner.id = 'LoadingSpinner';
    LoadingSpinner.className = 'DALPlayer-spinner';
    return LoadingSpinner;
}